"use strict";const { defineConfig } = require("prisma/config");
const path = require("path");

module.exports = defineConfig({
  schema: "prisma/schema.prisma",

  envFile: path.resolve(__dirname, ".env"),

  migrate: {
    datasource: "db",
    url: process.env.DATABASE_URL // 👈 THIS is what db push needs
  }
}); /* v9-99a100c62908d2f1 */
